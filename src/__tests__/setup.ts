// Vitest setup file
// Add any global test setup here

// Set locale for consistent Intl formatting in tests
// Thai locale formatting may differ across environments — utils tests
// use pattern matching instead of exact string comparison for Intl output.
